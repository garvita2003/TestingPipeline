# name: Triggered Workflow

# on:
#   repository_dispatch:
#     types: [start-workflow]

# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Do something
#         run: |
#           echo "Running tasks in Repo B"
#           sleep 120 # Sleep for 120 seconds (2 minutes)

name: Triggered Workflow

on:
  repository_dispatch:
    types: [start-workflow]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Ensure workflow always fails
        if: always()
        run: |
          echo "This is the final step, and it is designed to fail."
          exit 1

